"use client";
const AnotherComponent = ({ inputText }) => {
    // Use inputText as needed
    return (
      <div>
        <p>Input text from modal: {inputText}</p>
      </div>
    );
  };
  
  export default AnotherComponent;